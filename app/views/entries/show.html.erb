<% content_for :title do%>
      Foodlog &ndash; <%= DateTime.now.strftime("%B %e, %Y") %>
<% end %>

<%= params %>

<%= render @entry %>
<div class="columns mt-6 is-centered">
<div class="columns is-mobile">
  <div class="column">
    <div class="links">
      <%= link_to "Edit this entry", edit_entry_path(@entry), class: "button is-gray" %>
      <%= link_to "Back to entries", entries_path, class: "button is-gray" %> 
      <%= button_to "Delete", entry_path(@entry), method: :delete, form: { data: {turbo_confirm: 'Are you sure'}}, class: "button is-gray" %> 
      </div>

      <div class="column">
      </div>
    </div>
</div>
</div>
